{
  "name": "openclaw-ssh-proxy",
  "version": "1.0.0",
  "description": "SSH proxy service for Mission Control",
  "main": "ssh-proxy-server.js",
  "scripts": {
    "start": "node ssh-proxy-server.js"
  },
  "dependencies": {
    "ssh2": "^1.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
